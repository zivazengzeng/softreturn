import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.manmanhuilai.app",
  appName: "慢慢回来",
  webDir: "dist",
  ios: {
    contentInset: "always",
  },
};

export default config;
